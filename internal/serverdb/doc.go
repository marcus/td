// Package serverdb manages the sync server's SQLite database. It handles
// schema migrations, user and API key management, project CRUD, membership
// roles, device-code authentication flows, auth event logging, and
// paginated queries for server-side administration.
package serverdb
