# td-sync server configuration
# Copy to .env and adjust values as needed.

# Host port mapping (the container always listens on 8080 internally)
SYNC_LISTEN_PORT=8080

# Allow new user registration (set to "false" or "0" to disable)
SYNC_ALLOW_SIGNUP=true

# Public URL used for generating links (no trailing slash)
SYNC_BASE_URL=http://localhost:8080

# Graceful shutdown timeout (Go duration string)
SYNC_SHUTDOWN_TIMEOUT=30s

# --- S3 Litestream replication (uncomment and configure to enable) ---
# Also uncomment the S3 replica section in litestream.yml
# LITESTREAM_S3_BUCKET=my-bucket
# LITESTREAM_S3_ENDPOINT=https://s3.us-east-1.amazonaws.com
# AWS_DEFAULT_REGION=us-east-1
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
