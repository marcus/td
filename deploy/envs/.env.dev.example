# td-sync DEV environment
# Copy to .env.dev and customize as needed
# Run: ./deploy.sh dev

# === Server URL ===
SYNC_BASE_URL=http://localhost:8080

# === Server Config ===
SYNC_LISTEN_PORT=8080
SYNC_ALLOW_SIGNUP=true
SYNC_SHUTDOWN_TIMEOUT=10s
SYNC_LOG_FORMAT=text
SYNC_LOG_LEVEL=debug

# === Rate Limits (relaxed for dev) ===
SYNC_RATE_LIMIT_AUTH=100
SYNC_RATE_LIMIT_PUSH=600
SYNC_RATE_LIMIT_PULL=1200
SYNC_RATE_LIMIT_OTHER=3000

# === Litestream ===
# Dev uses file-based backup only (no S3 needed)
